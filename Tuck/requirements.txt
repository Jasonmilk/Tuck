# ==============================================
# Tuck - AI 会话版本控制 | 时间穿梭 | 多模型网关
# 完整版依赖清单 | 兼容 Python 3.9 ~ 3.12
# 已修复：无冲突、无报错、可直接安装
# ==============================================

# 核心 Web 框架
fastapi==0.115.6
uvicorn==0.32.1
uvloop==0.19.0
httpx==0.27.0

# 配置与数据校验
pydantic==2.10.4
pydantic-settings==2.8.1

# 异步文件 IO
aiofiles==24.1.0
filelock==3.16.1

# 安全与请求
python-multipart==0.0.12
cryptography==43.0.1

# 工具库
typing-extensions==4.12.2
python-dotenv==1.0.1

# 日志
structlog==24.4.0
colorlog==6.9.0

# 新增：Tuck 必须的系统依赖（防止启动失败）
anyio>=4.0.0
sniffio>=1.3.0
idna>=3.6
certifi>=2024.06.02
