# ==============================================
# Tuck - AI 会话版本控制 | 时间穿梭 | 多模型网关
# 完整版依赖清单 | 兼容 Python 3.9 ~ 3.12
# ==============================================

# 核心 Web 框架 (网关 + WebUI 必备)
fastapi==0.115.6
uvicorn==0.32.1
uvloop==0.19.0
httpx==0.27.0

# 配置与数据校验
pydantic==2.10.4
pydantic-settings==2.8.1

# 异步文件/IO 操作 (Personas 加载 + 内核存储)
aiofiles==24.1.0
filelock==3.16.1

# 安全与请求处理
python-multipart==0.0.12
cryptography==43.0.1

# 工具库 (兼容增强)
typing-extensions==4.12.2
python-dotenv==1.0.1

# 日志与调试 (生产级)
structlog==24.4.0
colorlog==6.9.0
